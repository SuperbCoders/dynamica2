.item.wrap-b id="item-#{item.id}"
  = form_for [@project, item], html: { class: 'form form-horizontal edit-item' }, remote: true do |f|  
    = link_to '×', project_item_path(@project, item), class: 'js-destroy-item btn-close', method: :delete, remote: true, data: { confirm: t('.destroy_confirmation') }
    .form-group
      h3.control-label.col-md-3.col-sm-3= item.name
      .col-md-7.col-sm-8
        .control-box
          = f.text_field :name, class: 'form-control', placeholder: 'Product name'
          = f.hidden_field :attachment_id, class: 'attachment_id'
        .control-box
          .file
            .selected style=('display:none' if item.values.empty?)
              .form-control.upload-box.fixed
                span.js-attachment-uploaded style='display:none' Data was successfully uploaded
                span.js-real-count #{item.values.count} values uploaded
                = link_to values_project_item_path(@project, item), method: :delete, remote: true, class: 'js-destroy-values btn-delete' do
                  i.icon.fa.fa-trash
            .not-selected style=('display:none' if item.values.any?)
              .form-control.upload-box
                | Drop file here
                a.js-choose-file href="#"  or choose
                |  from computer
            .loading style="display:none"
              .form-control.upload-box.loading
                  span.js-progress-bar.status-bar style="width:20%;" 
                  a.btn-cancel href="#"  Cancel
            .done style="display:none" OK
  = form_for [@project, Attachment.new] do |f|
    = f.file_field :file, style: 'visibility:hidden;', class: 'attachment_file'


- if item.with_parsing_errors
  coffee:
     $('[data-toggle="tooltip"]').tooltip()